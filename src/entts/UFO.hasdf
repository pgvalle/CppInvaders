#pragma once

#include "Shot.h"

struct UFO {
    enum State {
        AWAY,
        ALIVE,
        EXPLODING,
        DEAD // showing score value
    };

    State state;
    float x, vx, time;
    int score;

    UFO();

    void draw();
    void update(float delta);
    // bool collidedWithShot(const Shot *shot);
};
